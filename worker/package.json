{
  "name": "deeray-pipeline-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.3",
    "vitest": "^2.0.5",
    "wrangler": "^3.72.3",
    "@types/node": "^22.0.0"
  }
}
