import { RoutePlaceholder } from '../components/RoutePlaceholder';

export function ClientsView() {
  return (
    <RoutePlaceholder
      viewName="Clients"
      description="Client list and management. Available after client entity strategy is resolved."
    />
  );
}
