import { RoutePlaceholder } from '../components/RoutePlaceholder';

export function TodayView() {
  return (
    <RoutePlaceholder
      viewName="Today"
      description="Your daily priorities, follow-ups due today, and recent pipeline activity."
    />
  );
}
