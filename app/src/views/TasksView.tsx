import { RoutePlaceholder } from '../components/RoutePlaceholder';

export function TasksView() {
  return (
    <RoutePlaceholder
      viewName="Tasks"
      description="Follow-up actions, deadlines, and outstanding next steps."
    />
  );
}
